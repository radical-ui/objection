import SwiftUI

let lightBackgroundColor = Color(red: 255 / 255, green: 255 / 255, blue: 255 / 255)
let darkBackgroundColor = Color(red: 39 / 255, green: 39 / 255, blue: 39 / 255)

let lightForegroundColor = Color(red: 39 / 255, green: 39 / 255, blue: 39 / 255, opacity: 100 / 255)
let darkForegroundColor = Color(red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 100 / 255)

let noInternetHeader = "No Internet"
let noInternetContent = "This app requires an internet connection to function."

let errorHeader = "Uh oh"

let wsUrl = "ws://localhost:8000/ui"
