<script lang="ts">
	import { type Container } from '~/element_type'

	const {
		child,
		border,
		borderColor,
		borderRadius,
		childPosition,
		clip,
		color,
		height,
		padding,
		paddingBottom,
		paddingLeft,
		paddingRight,
		paddingTop,
		paddingX,
		paddingY,
		size,
		width,
	}: Container = $props()
</script>
