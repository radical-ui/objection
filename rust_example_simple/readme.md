# Objection Simple Example (Rust)

In one terminal, run the objection client.

```sh
objection run --bindings-path bindings.rs --engine-url http://localhost:8000
```

Then in another, run your engine.

```sh
cargo run
```
